# StreamTeX — Environment variables template
# Copy to .env and fill in your values. Never commit .env.

# --- Secrets ---
PYPI_TOKEN=               # PyPI publish token
STX_PASSWORD=             # Password gate sequence (e.g. hello, abc123)
